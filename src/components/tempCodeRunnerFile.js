
            setWatchlist(
                watchlist.concat({
                    title: props.title,
                    rating: props.rating,
                    genres: props.genres,
                    image: props.img
                })
            )